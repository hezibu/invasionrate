#include <Rcpp.h>
using namespace Rcpp;

// [[Rcpp::export]]
NumericMatrix repmatc(NumericVector a, double n, double m){
  NumericMatrix a1( 1 , a.size() , a.begin() );
  //NumericMatrix nm( n , m ).fill(1);
  arma::mat nm()
    return(nm);
}
